import 'package:flutter/material.dart';

import 'colors.dart';

final TraverDarkTheme =
    ThemeData.dark().copyWith(primaryColor: TraverColors.blackDark900);

    final TraverLightTheme =
    ThemeData.light().copyWith(primaryColor: TraverColors.blackLight400);
