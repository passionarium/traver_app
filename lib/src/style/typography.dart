import 'package:flutter/material.dart';

class TraverTypography {
  static const _masterStyle = TextStyle(
    fontFamily: 'Poppins',
  );

// ----------------
// Headline
// ----------------

//Headline900, bold, 56
  TextStyle headLine900Bold({Color? color}) => _masterStyle.copyWith(
        fontSize: 56,
        fontWeight: FontWeight.bold,
        color: color,
      );
//Headline900, w900, 48
  TextStyle headLine900({Color? color}) => _masterStyle.copyWith(
        fontSize: 48,
        fontWeight: FontWeight.w900,
        color: color,
      );
      TextStyle headLine900Bold({Color? color}) => _masterStyle.copyWith(
        fontSize: 56,
        fontWeight: FontWeight.bold,
        color: color,
      );

  TextStyle headLine900({Color? color}) => _masterStyle.copyWith(
        fontSize: 48,
        fontWeight: FontWeight.w900,
        color: color,
      );
      TextStyle headLine900Bold({Color? color}) => _masterStyle.copyWith(
        fontSize: 56,
        fontWeight: FontWeight.bold,
        color: color,
      );

  TextStyle headLine900({Color? color}) => _masterStyle.copyWith(
        fontSize: 48,
        fontWeight: FontWeight.w900,
        color: color,
      );
      TextStyle headLine900Bold({Color? color}) => _masterStyle.copyWith(
        fontSize: 56,
        fontWeight: FontWeight.bold,
        color: color,
      );

  TextStyle headLine900({Color? color}) => _masterStyle.copyWith(
        fontSize: 48,
        fontWeight: FontWeight.w900,
        color: color,
      );
      TextStyle headLine900Bold({Color? color}) => _masterStyle.copyWith(
        fontSize: 56,
        fontWeight: FontWeight.bold,
        color: color,
      );

  TextStyle headLine900({Color? color}) => _masterStyle.copyWith(
        fontSize: 48,
        fontWeight: FontWeight.w900,
        color: color,
      );
      TextStyle headLine900Bold({Color? color}) => _masterStyle.copyWith(
        fontSize: 56,
        fontWeight: FontWeight.bold,
        color: color,
      );

  TextStyle headLine900({Color? color}) => _masterStyle.copyWith(
        fontSize: 48,
        fontWeight: FontWeight.w900,
        color: color,
      );
      TextStyle headLine900Bold({Color? color}) => _masterStyle.copyWith(
        fontSize: 56,
        fontWeight: FontWeight.bold,
        color: color,
      );

  TextStyle headLine900({Color? color}) => _masterStyle.copyWith(
        fontSize: 48,
        fontWeight: FontWeight.w900,
        color: color,
      );
      TextStyle headLine900Bold({Color? color}) => _masterStyle.copyWith(
        fontSize: 56,
        fontWeight: FontWeight.bold,
        color: color,
      );

  TextStyle headLine900({Color? color}) => _masterStyle.copyWith(
        fontSize: 48,
        fontWeight: FontWeight.w900,
        color: color,
      );
      TextStyle headLine900Bold({Color? color}) => _masterStyle.copyWith(
        fontSize: 56,
        fontWeight: FontWeight.bold,
        color: color,
      );

  TextStyle headLine900({Color? color}) => _masterStyle.copyWith(
        fontSize: 48,
        fontWeight: FontWeight.w900,
        color: color,
      );
      TextStyle headLine900Bold({Color? color}) => _masterStyle.copyWith(
        fontSize: 56,
        fontWeight: FontWeight.bold,
        color: color,
      );

  TextStyle headLine900({Color? color}) => _masterStyle.copyWith(
        fontSize: 48,
        fontWeight: FontWeight.w900,
        color: color,
      );
}
